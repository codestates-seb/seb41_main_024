import { createTheme } from '@mui/material/styles';

const basicTheme = createTheme({
  palette: {
    primary: {
      main: '#63A8DA',
      contrastText: '#fff',
    },
  },
});

export default basicTheme;
